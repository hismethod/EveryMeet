<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="kr.ac.kit.views.Activities.CreateRoomActivity" >

    <EditText
        android:id="@+id/createRoomTitleEdit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="회의실 이름"
        android:inputType="text"
        android:textSize="25sp" />

    <EditText
        android:id="@+id/createRoomPasswordEdit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/createRoomTitleEdit"
        android:hint="비밀번호"
        android:inputType="textPassword"
        android:privateImeOptions="defaultInputmode=english;symbol=false"
        android:textSize="12sp" />

    <EditText
        android:id="@+id/createRoomPasswordConfirmEdit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/createRoomPasswordEdit"
        android:hint="비밀번호확인"
        android:inputType="textPassword"
        android:privateImeOptions="defaultInputmode=english;symbol=false"
        android:textSize="12sp" />

    <com.gc.materialdesign.views.ButtonFlat
        android:id="@+id/createRoomSubmitBtn"
        android:layout_width="60dp"
        android:layout_height="48dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:gravity="center_vertical|center_horizontal"
        android:text="개설"
        android:textAlignment="center"
        android:textStyle="bold"/>

    <com.gc.materialdesign.views.ButtonFlat
        android:id="@+id/createRoomCancelBtn"
        android:layout_width="60dp"
        android:layout_height="48dp"
        android:layout_alignParentBottom="true"
        android:layout_toLeftOf="@+id/createRoomSubmitBtn"
        android:gravity="center_vertical|center_horizontal"
        android:background="@android:color/black"
        android:text="취소"
        android:textAlignment="center"
        android:textStyle="bold"/>

</RelativeLayout>